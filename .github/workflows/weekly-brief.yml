# Re-registering the schedule trigger (temp every-10-min test).
name: Weekly Pipeline Brief

on:
  schedule:
    # TEMP schedule test — fires every 10 minutes.
    # TODO: restore to "0 12 * * 1" (Mondays 12:00 UTC, ~7-8am US Eastern) after the test.
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  send-brief:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run brief
        env:
          HUBSPOT_TOKEN: ${{ secrets.HUBSPOT_TOKEN }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          BRIEF_FROM_EMAIL: ${{ secrets.BRIEF_FROM_EMAIL }}
          BRIEF_TO_EMAIL: ${{ secrets.BRIEF_TO_EMAIL }}
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python runner.py

      - name: Upload HTML artifact
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: pipeline-brief-html
          path: |
            dsn-pipeline-brief-live.html
            dsn-pipeline-brief-email.html
          if-no-files-found: ignore
